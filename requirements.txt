# Per-row gradient interference — pinned dependencies
# Python 3.10+ (tested on 3.10/3.11). Install torch matching your CUDA first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements.txt

# --- core ---
torch>=2.4,<2.10          # bf16 autocast + scaled_dot_product_attention (is_causal)
numpy>=1.24,<2.2
scipy>=1.11               # stats.py: Welch's t-test, Cohen's d

# --- data / tokenizer ---
tiktoken>=0.7             # GPT-2 BPE
datasets>=2.19            # FineWeb streaming

# --- optimizer (PIN THIS) ---
# Muon is installed from git and is UNVERSIONED on PyPI, so it must be pinned to
# a commit or the optimizer changes under a future install.
#
# The pin below is INFERRED, not read off the training environment: the training
# instance was set up 2026-05-18 (results/.instance_log), so training used Muon
# HEAD as of that date, and 056a3c5869cf (2026-04-15) is the last upstream commit
# at or before it. If the training box is reachable, confirm with `pip show muon`.
git+https://github.com/KellerJordan/Muon@056a3c5869cf#egg=muon
